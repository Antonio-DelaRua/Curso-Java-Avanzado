package Nombrado;

public class Main {
    public static void main(String[] args) {

    }
}
    class ReverseString{ }

    class UserManager{
    private int contador = 0;
    public void createUser(){
        int[]numero = {1,2,3,4,5,6,7};
        boolean diaONoche;
    }
    }

    interface Itetable{ }

    interface Seriezable{}

    interface Iterador{}

    interface Serializador{}



/**
 * nombrado consiste en dar un nombre claro que identifique a aquello que se lo estamos otorgando
 *
 * las constantes se escriben en mayusculas separadas por _ ej:(MAX_USERS)
 *
 * los nombres de la variables tienen q ser autodescriptivos.
 *
 * las variables al igual que en los metodos se utiliza el camellcase.
 *
 * Anotacion hungara: consiste en que se pone 1 o 2 letras al nombre de una variable y que posteriormente
 * nos va a ayudar a identificarla solamente con verla aunque no sepamos el tipo de dato.
 * ej: public void createUser(){
 *     int[] aNumeros = {1,2,3,}  ---> int letra a
 *     boolean bEsdeDia = false;  ---> boolean letra b
 *     Char cLetra = 'a';         ---> Char letra a
 *     double dvalor = 5.2;       ---> double letra d
 *     f ->                       --->funcion f
 *     Hastable<String, Integer> husuarios = new Hastable()   ---> letra h
 *
 *     int ivalor = 5;
 *     long iValoresLong = 5 ;
 *
 *     Enum de anotacion Hungara
 *
 *     enum nEstado{
 *      INITIAL,
 *      EN_PROCESS
 *      };
 *      si instanciamos objetos utilizariamos la letra o.
 *      String sCadena = "hola mundo ";  //-> String letra s
 *      byte yByte = 1; //-> byte letra y
 *      byte byByte = 1 //-> byte letra by
 *      float fNum = 1.5f; //-> float letra f
 *      float flNum = 1.5f; //-> float letra fl
 *      JButton btnBoton; //-> JButton btn
 *      JLabel lblArco; //-> JLabel lbl
 *
 *
 *
 *
 *
 * }
 *
 *
 */